# mapbox — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png` (this directory)
- **Generated with**: built-in `imagegen` fallback — a Python/Pillow generator
  (`/tmp/hg/gen_cover.py`), used because no ImageGen tool is available in-session
  (recorded in workspace memory as the established cover fallback).
- **Tool mode**: programmatic raster generation (Pillow 10.4.0), `SMOOTH_MORE`
  finish.

## Composition

A deep teal-night field standing for a map canvas. A faint graticule of
meridians/parallels curves across it. A glowing route polyline arcs from
lower-left to upper-right with waypoint nodes, and two location pins anchor
origin and destination — a literal map-with-route. A soft compass rose glows
upper-right. No text.

## Palette

- Field: teal-night `#06201f` → near-black `#021310`
- Accent (route / pins): mapbox-cyan `#4bb4d6`
- Accent (waypoints / compass): warm amber `#f4b942`

## Acceptance notes

- 3:2 landscape at 1536 × 1024; subject held within the central safe area.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (map-canvas-with-route-polyline
  motif is unique to `mapbox`).
