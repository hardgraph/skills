# chrome-devtools — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png`
- **Generated with**: built-in `imagegen` fallback — Python/Pillow generator
  (`/tmp/hg/gen_cover.py`); no ImageGen tool available in-session (per workspace
  memory). Pillow 10.4.0, `SMOOTH_MORE` finish.

## Composition

A stacked, layered "inspection" tableau on a dark slate field: a browser window
frame (traffic-light dots) contains a tower of colour-coded element layers being
inspected, with a ruler/tick scale along the top edge and three soft colour
glows. Evokes selecting and drilling into nested DOM layers. No text.

## Palette

- Field: slate `#171c28` → near-black `#0c0f18`
- Accents: Chrome blue `#4285f4`, green `#34a853`, amber `#fbc042`, pink `#e96382`

## Acceptance notes

- 3:2 at 1536 × 1024; central safe area respected.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (layered-inspection tableau is
  unique to `chrome-devtools`).
