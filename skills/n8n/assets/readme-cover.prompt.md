# n8n — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png`
- **Generated with**: built-in `imagegen` fallback — Python/Pillow generator
  (`/tmp/hg/gen_cover.py`); no ImageGen tool available in-session (per workspace
  memory). Pillow 10.4.0, `SMOOTH_MORE` finish.

## Composition

On a deep-violet field, a **workflow node network**: a grid of rounded operator
nodes wired together by edges in an irregular mesh, evoking a visual workflow
graph of connected steps — the node-based automation idea. Bright node pills sit
on soft red and indigo glows. No text.

## Palette

- Field: violet `#161226` → near-black `#0a0816`
- Accents: red `#ff5078`, indigo `#786eff`, node cream `#f5f0ff`, edge lavender

## Acceptance notes

- 3:2 at 1536 × 1024; central safe area respected.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (operator-node mesh is unique
  to `n8n`).
