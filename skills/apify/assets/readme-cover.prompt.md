# apify — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png` (this directory)
- **Generated with**: built-in `imagegen` fallback — a Python/Pillow generator
  (`/tmp/hg/gen_cover.py`), used because no ImageGen tool is available in-session
  (recorded in workspace memory as the established cover fallback).
- **Tool mode**: programmatic raster generation (Pillow 10.4.0), `SMOOTH_MORE`
  finish.

## Composition

A deep indigo field. A web of interlinked **nodes** at the centre stands for the
scraped link graph; an outer ring of small worker tiles represents parallel
Actor runs fanning out from one input. A single warm input packet enters from the
left; cool output streams leave to the right, standing for the dataset export.
No text.

## Palette

- Field: indigo `#0f1230` → near-black `#05061a`
- Accent (link graph / input): magenta `#e056d6`
- Accent (workers / output): cyan `#34d6e8`

## Acceptance notes

- 3:2 landscape at 1536 × 1024; subject held within the central safe area.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (link-graph-with-fan-out-workers
  motif is unique to `apify`).
