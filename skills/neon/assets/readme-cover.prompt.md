# neon — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png` (this directory)
- **Generated with**: built-in `imagegen` fallback — a Python/Pillow generator
  (`/tmp/hg/gen_cover.py`), used because no ImageGen tool is available in-session
  (recorded in workspace memory as the established cover fallback).
- **Tool mode**: programmatic raster generation (Pillow 10.4.0), `SMOOTH_MORE`
  finish.

## Composition

A dark navy field with a deep vertical gradient. From a bright root node at the
upper-left, an electric-green copy-on-write **branching graph** radiates outward
in a breadth-first tree — a literal depiction of Neon's database branching:
forks that share history and only store the pages they change. Cool-blue and
green glow pools anchor the field. No text.

## Palette

- Field: navy `#080c1a` → near-black `#03050f`
- Accent (branches/nodes): electric mint-green `#3ad3a5`
- Secondary glow: blue `#3884ff`

## Acceptance notes

- 3:2 landscape at 1536 × 1024; subject held within the central safe area.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (branching-tree motif is
  unique to `neon`).
