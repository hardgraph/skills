# appwrite — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png`
- **Generated with**: built-in `imagegen` fallback — Python/Pillow generator
  (`/tmp/hg/gen_cover.py`); no ImageGen tool available in-session (per workspace
  memory). Pillow 10.4.0, `SMOOTH_MORE` finish.

## Composition

A modular **stacked-blocks** mosaic on a dark field: differently-sized rounded
service blocks (auth, database, storage, functions, messaging, realtime)
arranged like a backend that composes from parts — the backend-as-a-service idea
made literal. Warm red/pink Appwrite-toned palette with secondary orange, teal,
and violet blocks. No text.

## Palette

- Field: dark `#1c101a` → near-black `#0e080e`
- Accents: red-pink `#ff486d`, orange `#ff7842`/`#ff9646`, support teal/violet

## Acceptance notes

- 3:2 at 1536 × 1024; central safe area respected.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (modular service-block mosaic
  is unique to `appwrite`).
