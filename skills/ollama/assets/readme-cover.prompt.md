# ollama — readme-cover

- **Asset**: `readme-cover.png` (1536 × 1024, 3:2, PNG, RGB)
- **Selected asset**: `./readme-cover.png` (this directory)
- **Generated with**: built-in `imagegen` fallback — a Python/Pillow generator
  (`/tmp/hg/gen_cover.py`), used because no ImageGen tool is available in-session
  (recorded in workspace memory as the established cover fallback).
- **Tool mode**: programmatic raster generation (Pillow 10.4.0), `SMOOTH_MORE`
  finish.

## Composition

A dark slate field with a soft vertical gradient. A single luminous **neural
node** at the centre radiates concentric signal rings outward — a literal take
on one model serving many callers. A warm amber accent on one ring stands for
the `:cloud` route; the rest glow cool teal, standing for local inference. No
text.

## Palette

- Field: slate `#0c1118` → near-black `#04070b`
- Accent (local/rings): teal `#33d6c2`
- Accent (cloud ring): amber `#f2a93b`

## Acceptance notes

- 3:2 landscape at 1536 × 1024; subject held within the central safe area.
- No watermark, no provider marks, no generated text.
- Distinct from every other hardgraph skill cover (concentric-signal-ring motif
  is unique to `ollama`).
